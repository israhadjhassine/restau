<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des restaurants - FoodDelivery Admin</title>
    <style>
        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
        }
        
        body {
            font-family: 'Arial', sans-serif;
            background: #f8f9fa;
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }
        
        /* Navbar Admin */
        .navbar { 
            background: #2c3e50; 
            padding: 1rem 2rem; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            color: white; 
            position: sticky;
            top: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
        }
        
        .logo h2 a {
            color: white;
            text-decoration: none;
            font-size: 1.5rem;
        }
        
        .nav-links { 
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        
        .nav-links a { 
            color: white; 
            text-decoration: none; 
            padding: 0.5rem 1rem; 
            border-radius: 4px; 
            transition: all 0.3s; 
            font-weight: 500;
            font-size: 0.95rem;
        }
        
        .nav-links a:hover { 
            background: rgba(255,255,255,0.15); 
            transform: translateY(-2px);
        }
        
        .nav-links a.active { 
            background: #e44d26;
        }
        
        .logout-btn {
            background: #e44d26;
            padding: 0.5rem 1.5rem;
            border-radius: 4px;
        }
        
        .logout-btn:hover {
            background: #c63d1a;
        }
        
        /* Content Container */
        .content-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
            padding-top: 100px;
        }
        
        /* Header */
        .page-header {
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 2px solid #eee;
        }
        
        .page-header h1 {
            color: #2c3e50;
            font-size: 2.2rem;
            margin-bottom: 0.5rem;
        }
        
        .page-header p {
            color: #666;
            font-size: 1.1rem;
        }
        
        /* Stats Banner */
        .stats-banner {
            background: white;
            border-radius: 12px;
            padding: 1.5rem 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 3px 15px rgba(0,0,0,0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .stat-item {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .stat-icon {
            width: 50px;
            height: 50px;
            background: #e44d26;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }
        
        .stat-content h3 {
            font-size: 1rem;
            color: #666;
            margin-bottom: 0.3rem;
        }
        
        .stat-value {
            font-size: 1.8rem;
            font-weight: bold;
            color: #2c3e50;
        }
        
        /* Table Container */
        .table-container {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            margin-bottom: 2rem;
        }
        
        .table-header {
            padding: 1.5rem 2rem;
            background: #2c3e50;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .table-header h2 {
            font-size: 1.5rem;
            margin: 0;
        }
        
        .search-box {
            display: flex;
            gap: 0.5rem;
        }
        
        .search-box input {
            padding: 0.8rem 1rem;
            border: none;
            border-radius: 6px;
            width: 250px;
            font-size: 0.95rem;
        }
        
        .search-btn {
            background: #e44d26;
            color: white;
            border: none;
            border-radius: 6px;
            padding: 0 1.2rem;
            cursor: pointer;
            font-weight: 500;
            transition: background 0.3s;
        }
        
        .search-btn:hover {
            background: #c63d1a;
        }
        
        /* Table */
        .data-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .data-table th {
            background: #f8f9fa;
            padding: 1.2rem 1.5rem;
            text-align: left;
            font-weight: 600;
            color: #2c3e50;
            border-bottom: 2px solid #eee;
            font-size: 0.95rem;
        }
        
        .data-table td {
            padding: 1.2rem 1.5rem;
            border-bottom: 1px solid #eee;
            color: #555;
        }
        
        .data-table tr:hover {
            background: #f9f9f9;
        }
        
        .data-table tr:last-child td {
            border-bottom: none;
        }
        
        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }
        
        .btn {
            padding: 0.6rem 1.2rem;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 500;
            font-size: 0.9rem;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn-danger {
            background: #dc3545;
            color: white;
        }
        
        .btn-danger:hover {
            background: #c82333;
            transform: translateY(-2px);
        }
        
        .btn-edit {
            background: #17a2b8;
            color: white;
        }
        
        .btn-edit:hover {
            background: #138496;
            transform: translateY(-2px);
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #666;
        }
        
        .empty-icon {
            font-size: 4rem;
            color: #ddd;
            margin-bottom: 1.5rem;
        }
        
        .empty-state h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: #555;
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .content-container {
                padding: 1.5rem;
                padding-top: 90px;
            }
            
            .table-header {
                flex-direction: column;
                gap: 1rem;
                align-items: stretch;
            }
            
            .search-box input {
                width: 100%;
            }
        }
        
        @media (max-width: 768px) {
            .data-table {
                display: block;
                overflow-x: auto;
            }
            
            .stats-banner {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .nav-links {
                display: none;
            }
            
            .action-buttons {
                flex-direction: column;
            }
        }
        
        @media (max-width: 480px) {
            .content-container {
                padding: 1rem;
                padding-top: 80px;
            }
            
            .page-header h1 {
                font-size: 1.8rem;
            }
            
            .stat-icon {
                width: 40px;
                height: 40px;
                font-size: 1.2rem;
            }
            
            .stat-value {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="logo">
            <h2><a href="/admin">FoodDelivery Admin</a></h2>
        </div>
        <div class="nav-links">
            <a href="/admin/dashboard">Dashboard</a>
            <a href="/admin/inscriptions">Inscriptions</a>
            <a href="/admin/restaurants" class="active">Restaurants</a>
            <a href="#">Utilisateurs</a>
            <a href="#">Commandes</a>
            <a href="/logout" class="logout-btn">Déconnexion</a>
        </div>
    </nav>

    <!-- Content -->
    <div class="content-container">
        <!-- Header -->
        <div class="page-header">
            <h1>Gestion des restaurants</h1>
            <p>Consultez et gérez tous les restaurants de la plateforme</p>
        </div>

        <!-- Stats Banner -->
        <div class="stats-banner">
            <div class="stat-item">
                <div class="stat-icon">🏪</div>
                <div class="stat-content">
                    <h3>Restaurants total</h3>
                    <div class="stat-value">
                        <% if (restaurants && restaurants.length) { %>
                            <%= restaurants.length %>
                        <% } else { %>
                            0
                        <% } %>
                    </div>
                </div>
            </div>
            <div class="stat-item">
                <div class="stat-icon">✅</div>
                <div class="stat-content">
                    <h3>Actifs</h3>
                    <div class="stat-value">
                        <% if (restaurants && restaurants.length) { %>
                            <%= restaurants.length %>
                        <% } else { %>
                            0
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Table -->
        <div class="table-container">
            <div class="table-header">
                <h2>Restaurants inscrits</h2>
                <div class="search-box">
                    <input type="text" placeholder="Rechercher un restaurant..." id="searchInput">
                    <button class="search-btn">🔍</button>
                </div>
            </div>
            
            <% if (restaurants && restaurants.length > 0) { %>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Nom Restaurant</th>
                            <th>Email</th>
                            <th>Adresse</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% restaurants.forEach(r => { %>
                        <tr>
                            <td><strong><%= r.nomRestaurant %></strong></td>
                            <td><%= r.email %></td>
                            <td><%= r.adresse || "-" %></td>
                            <td>
                                <div class="action-buttons">
                                    <form action="/admin/restaurants/delete/<%= r._id %>" method="POST">
                                        <button type="submit" class="btn btn-danger" onclick="return confirmDelete()">
                                            🗑 Supprimer
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon">🏪</div>
                    <h3>Aucun restaurant inscrit</h3>
                    <p>Aucun restaurant n'est actuellement enregistré sur la plateforme.</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        // Search functionality
        document.getElementById('searchInput').addEventListener('keyup', function(e) {
            const searchTerm = e.target.value.toLowerCase();
            const rows = document.querySelectorAll('.data-table tbody tr');
            
            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                row.style.display = text.includes(searchTerm) ? '' : 'none';
            });
        });
        
        // Confirmation dialog for delete
        function confirmDelete() {
            return confirm('Êtes-vous sûr de vouloir supprimer ce restaurant ?');
        }
    </script>
</body>
</html>